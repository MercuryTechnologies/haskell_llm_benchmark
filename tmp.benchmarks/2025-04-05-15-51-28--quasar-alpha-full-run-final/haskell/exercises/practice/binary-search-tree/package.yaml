name: binary-search-tree
version: 1.0.0.4

dependencies:
  - base

library:
  exposed-modules: BST
  source-dirs: src
  ghc-options: -Wall

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - binary-search-tree
      - hspec
